---
// SiteNav.astro - Tab-style navigation linking /about and /links

interface Props {
	activePage: 'about' | 'links'
}

const { activePage } = Astro.props
---

<nav class="site-nav" aria-label="Site navigation" transition:name="site-nav">
	<div class="tabs-list">
		<a
			href="/about"
			class="tab-link"
			transition:name="tab-about"
			aria-current={activePage === 'about' ? 'page' : undefined}
		>
			About
		</a>
		<a
			href="/links"
			class="tab-link"
			transition:name="tab-links"
			aria-current={activePage === 'links' ? 'page' : undefined}
		>
			Links
		</a>
	</div>
</nav>
